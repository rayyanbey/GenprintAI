import { DesignToCart } from '@/components/Products/DesignToCart';

export const metadata = {
  title: 'Design to Cart | Genprint AI',
  description: 'Create custom merchandise with your saved designs',
};

export default function DesignToCartPage() {
  return <DesignToCart />;
}
